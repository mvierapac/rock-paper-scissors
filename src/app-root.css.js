import { css } from 'lit';

export default css`
  .app {
    font-family: Arial, sans-serif;
    height: 100vh;
    position: relative;
    max-width: 1200px;
    margin-inline: auto;
    padding-inline: 20px;
  }

  #game {
    align-items: center;
    display: flex;
    justify-content: center;
    min-height: 100vh;
    text-align: center;
  }
`;
